'use client';

export default function Paises(): JSX.Element {
  const newLocal = "bg-white h-screen w-full";
	return (
    <div className={newLocal}>
    <p className="text-black">paises</p>
  </div>
	);
}