'use client';

export default function Empresas(): JSX.Element {
  const newLocal = "bg-white h-screen w-full";
	return (
    <div className={newLocal}>
    <p className="text-black">empresas</p>
  </div>
	);
}