'use client';

export default function Movimientos(): JSX.Element {
  const newLocal = "bg-white h-screen w-full";
	return (
    <div className={newLocal}>
    <p className="text-black">movimientos</p>
  </div>
	);
}